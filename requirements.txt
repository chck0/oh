# ── 데이터 파이프라인 (scripts/) ─────────────────────────────
aiohttp>=3.9
python-dotenv>=1.0
requests>=2.31

# ── FastAPI 백엔드 (app/) ────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# ── LLM ──────────────────────────────────────────────────────
anthropic>=0.40

# ── 파일 첨부 파싱 (spec-27) ─────────────────────────────────
# python-docx / python-pptx → lxml(C 확장) 의존으로 Vercel 빌드 불가 → 제외
pypdf>=4.0                    # PDF 텍스트 추출 (순수 Python, Vercel 호환)

# ── Supabase / Postgres (Vercel 런타임에서 사용) ─────────────
# psycopg[binary] — Vercel 빌드 환경에 wheel 제공 (gcc 빌드 불필요)
psycopg[binary]>=3.1

# ── 테스트 (로컬 dev 전용) ────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
aioresponses>=0.7
httpx>=0.27                # FastAPI TestClient 의존성
