## 변경 요약

### spec-16 — 가격 변동률 배지
- 카드에 최근 3개월 vs 이전 6개월 평균 실거래가 비교 배지 표시 (▲▼)
- trade_history 배치 조회, 3% 미만 노이즈 필터, graceful degradation
- 비교 테이블에 "6개월 변동" 행 추가

### spec-17 — 단지 상세 모달
- 카드 클릭 → 바텀시트 모달 (단지정보 / 시세·거래 / 주변시설 3탭)
- GET /api/apt/{apt_seq}/detail 기존 엔드포인트 재사용
- _detailCache 캐시, ESC/배경클릭/✕ 닫기, body scroll 잠금
- W2 히스토리 칩 버그픽스: _buildChipsHtml() 헬퍼 + recent-searches-2 컨테이너

### spec-18 — 경로 상세 팝업
- 카드 통근시간 칩 클릭 → step-by-step 경로 바텀시트 (z-index: 700)
- GET /api/apt/{apt_seq}/routes 기존 엔드포인트 재사용
- 교통수단 아이콘(도보/버스/지하철) + 노선 + 구간 + 소요시간
- dual 모드 W1/W2 탭, _routeCache 캐시
- ESC 우선순위: route → detail → compare

## 테스트
- 380 tests passed (회귀 없음)
- 커버리지: 83%

🤖 Generated with [Claude Code](https://claude.com/claude-code)
