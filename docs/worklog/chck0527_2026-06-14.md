## 2026-06-14 후속6 — 버스 라우팅 쌍→시퀀스 (루프 해소)

**문제:** 쌍 단위 라우팅이 정류장마다 U턴·이음새 꺾임·블록 루프를 대량 발생(실제 버스 경로 아님).
**원인:** ①정류장 쌍을 따로 라우팅→각 정류장서 OSRM이 차선맞춤 U턴 ②쌍 이어붙일 때 꺾임 ③OSRM 차경로라 일방통행 우회.
**해법:** PoC처럼 **버스 step 전체 정류장을 한 번에 라우팅**(continue_straight) + detour 1.8배 컷→직선fallback.
- `route_bus_seq.py`/`backfill_bus_seq.py` 신규, `bus_seq_geom`(시퀀스→도로곡선). 원본 정류장은 raw JSON에서(transit_routes는 이미 도로좌표).
- 결과: ok 25,611(85%)/straight 4,455. backfill 32,415행. 루프의심 1%, pytest 388 passed.
- (구) 쌍 기반 build_bus_pairs/route_bus_pairs/backfill_bus_geom·bus_pair_geom은 **superseded**.

---

## 2026-06-14 후속5 — 버스 도로 라우팅 (OSRM, Spec 32)

**의도:** 버스 경로를 정류장 직선 연결 → 도로 따라가는 곡선으로 (지하철처럼). 전체+100%, baked.

**방식:** 노선 형상 데이터는 공개된 게 없음 → **정류장을 도로에 라우팅**(OSRM). 노선번호/이름 매칭 불필요 → 지하철보다 단순. 쌍 단위 캐시(`bus_pair_geom`)로 재사용.

**최종 상태:** 완료.
- 로컬 OSRM(Docker, south-korea OSM, MLD) 구축
- 고유 무방향 정류장 쌍 13,306개 라우팅: **ok 11,557(86%) / straight fallback 1,749** (검증 `_route_ok`: detour 2.6배·내부점프 컷)
- backfill: 버스 step → 쌍 이어붙여 도로곡선. 버스 평균 180점.
- 프론트 변경 0 (simplify+smooth가 자동 적용)

**건드린 파일:** scripts/migrate_bus_pair.sql, build_bus_pairs.py, route_bus_pairs.py, backfill_bus_geom.py (신규), config.py(OSRM_URL)

**함정·깨진 것:**
- **버스 분류 버그**: 처음 `'호선' 없음`으로 거름 → 신림선·경의중앙선·신분당선 등 **비-호선 철도를 버스로 오인**해 도로로 덮음. → `step_type=='버스'`로 정정. 덮인 철도는 backfill_pair_geom(raw) + fill_null_subway(전체 지하철 GTFS 트랙 재도출)로 **복원**.
- lng/lat 순서(OSRM은 lng,lat) — 우리 좌표가 이미 lng,lat라 OK

**검증:** 신림선 트랙 복원 확인(저장==트랙재도출), 지하철 136점·버스 180점, pytest 388 passed.

**미완성/TODO:**
- straight fallback 1,749쌍(13%): OSRM 우회/실패. 원인별 조사 시 더 줄일 수 있음
- `.tmp/osrm` ~677MB(gitignore): 재라우팅용 보존, 불필요시 삭제
- 운영(Supabase) 동기화는 별도 (지하철과 공통 과제)

---

## 2026-06-14 후속4 — 전체 노선망 전수 조사 + 응암루프 수동 보정

**의도:** transit_routes(검색분)가 아니라 **GTFS 전체 수도권 노선·역**으로 전수 조사.

**발견·수정:**
- 전체 1,688개 인접 역쌍 중 **1개만 직선**: 6호선 **응암→역촌**(응암루프). OSM이 왕복 선로를 뭉쳐놔 직접 0.91km 구간 자동 추출 불가(슬라이스가 4.72km 루프-역방향).
- **수동 보정**: 축 수직 부호로 직진/귀환 선로 분리 → 직진 9pts 추출 → `data/subway_pair_overrides.json`(양방향)에 저장. `build_pair_geom._apply_overrides`가 재빌드 시 자동 적용.
- **다중 stop_id 환승역 보정**: `_pair_byname`(역명 기반 pair 폴백) 추가 — stop_id가 여러 개라 정확 매칭 실패 시 역명으로 곡선 찾음.
- `fill_null_subway`: NULL뿐 아니라 2pt 직선도 재처리(override 곡선 반영).

**최종 (전체 40,263 step):** NULL 0, 결함>1.5km 0, 곡선 99.91%. 응암→역촌 11pts 곡선 렌더 확인. pytest 388 passed.

**남은 36 step(9쌍, 0.09%):** 양평→영등포구청·학동→논현 등 — OSM이 해당 구간에 **2점만 제공**(직선 선로/OSM sparse). ≤1km 인접역 직선으로, OSM 데이터 기준 정확한 표현(곡선 데이터 자체가 없음).

---

## 2026-06-14 후속3 — 전수 재검증: 수인분당선/별내선/GTX-A/NULL 처리 (100% 목표)

**의도:** "안 그려지거나 에러나거나 넘어간 것" 전수 조사. 점프 스캔이 아니라 step별 스티칭 결과를 이유별로 분류.

**발견·수정:**
1. **수인분당선 996 step(3%) 누락** — `_norm`이 마침표(`.`)를 안 떼서 "수인.분당선"≠GTFS"수인분당선". → `_STRIP_SPECIAL`에 `.`/`,`/`/` 추가. 996 step 곡선 복구.
2. **별내선·GTX-A 북부** — GTFS 미수록이지만 OSM엔 geometry 있음(8호선·a선 키). → `enrich_odsay_pairs` 신규: GTFS 실패 시 **ODsay 역 순서 + OSM 곡선**으로 2차 폴백 (별내선 158pts 곡선 복구). `_curve_ok`로 미span(ratio 0.1)·텔레포트 컷.
3. **NULL(미렌더) 지하철 step 2255개** — raw JSON 없는 셀(wp9 282·wp14 117 등)을 backfill이 못 채움. → `build_subway_by_name` 신규 + `fill_null_subway.py`: transit_routes의 출발/도착 역명으로 GTFS 좌표 찾아 스티칭(raw 불필요). **24,957 step 채움**.

**최종 결과 (전체 40,263 지하철 step) — 100%:**
- 결함(>1.5km 점프) **0**, 순수직선 **0**, NULL **0** → **곡선 정상 40,263 (100.00%)**
- 마지막 별내선 NULL은 raw 없는 99셀 재수집(`.tmp/recollect_nullcells.py`, fetch_cells) → backfill enrich로 해소. 별내선 장자호수공원→석촌 120pts 곡선(점프 0.62km)
- pytest 388 passed

**재현 순서:** migrate_gtfs_subway.sql → build_gtfs_subway.py → reconcile_lines.py → build_pair_geom.py → (raw 없는 셀 재수집) → backfill_pair_geom.py → fill_null_subway.py

**건드린 파일:** `app/subway_shapes.py`(_STRIP_SPECIAL), `app/gtfs_subway.py`(_line_key/_osm_keys/enrich_odsay_pairs/build_subway_by_name/스냅가드), `scripts/backfill_pair_geom.py`(2차 폴백), `scripts/fill_null_subway.py`(신규)

---

## 2026-06-14 후속2 — GTFS 시퀀스 불완전(단축운행 대표) 수정

**의도:** way-chaining 후에도 7호선 등에서 직선 구간 다수 잔존. 재검증 결과 결함이 13개 distinct 역쌍(1442 step)으로 좁혀졌고, 7호선 3쌍(내방→고속터미널 등 1135 step)이 압도적.

**원인:** `build_gtfs_subway.py`가 노선당 대표 트립으로 **`_Ord001`(첫 운행회차)**를 썼는데, 이게 **단축운행**일 수 있음. 7호선 _Ord001 = 12역인데 전구간 트립 = **53역** → 내방·고속터미널 등이 시퀀스에서 통째로 빠져 스티칭 실패 → 직선.

**수정:** `load_sequences`를 노선당 **정차 최다 트립(전구간 운행) 선택**으로 교체. (RR 철도 트립만 메모리 보관 후 route별 max-stop 트립 채택)

**결과 (재검증, wp 3/7/9/14, 13228 step):**
- seq 3865 → 5593행. 7호선 12역 → **53역**, 곡선 커버리지 2호선 114→154, 3호선 52→86
- 결함 step **1442 → 72 (0.5%)**. 7호선/기타 9쌍 **전부 해소**
- 7호선 내방→고속터미널 29pts 점프 0.47km ✅
- pytest 388 passed

**남은 결함 = 8호선 별내선 4쌍(95 step)뿐** — GTFS 2024.3 미수록(별내선 2024.8 개통). 데이터 한계.

---

## 2026-06-14 후속 — OSM way-chaining으로 텔레포트/직선/스파이크 근본 해결

**의도:** Spec 31 적용 후에도 (1)어떤 역간은 곡선·어떤 역간은 직선, (2)삐져나옴/끊김 발생. 원인 추적 결과 **OSM `subway_shapes_kr.json`의 way 조각들이 노선 순서대로 안 꿰어져 있어** `_flat_pts`가 naive concat할 때 way 경계에서 2~12km 텔레포트가 생겼고, 이걸 `_curve_ok`가 거부하면 직선·통과시키면 스파이크가 됐음.

**커뮤니티 조사:** GitHub/공공데이터 전수 조사 결과 깔끔한 "노선 선형" 데이터셋은 사실상 없음 (전부 역 점+그래프, 선로 곡선은 OSM이 유일 출처). 즉 데이터는 충분하고 **정렬만 하면 됨**.

**최종 상태:** 완료. pytest **388 passed, 0 failed** (이전 21개 search 실패도 signature backfill로 transit_routes 정상화되며 자동 통과).

**건드린 파일·함수:**
- `app/subway_shapes.py`: `_flat_pts` → `_chain_ways`(끝점 매칭 그리디 체이닝) 버전으로 교체 + shape별 메모이즈. 가장 긴 way에서 시작해 양방향으로 끝점 맞는 way를 (필요시 뒤집어) 연결, 안 붙는 조각은 버림.
- `app/gtfs_subway.py`: `_line_key`(운행종별 접미사 급행/특급/직통 제거 → "9호선급행"→"9호선") + 스냅 거리 가드(`_SNAP_TOL2`, 역이 GTFS에 없으면 ODsay 폴백).

**검증 (wp 3,7,9,14, 13228 step):**
- 내부 텔레포트: 2호선 shape 12km → 0.3km. 4호선 충무로→이촌 최대점프 1.91km → **0.69km**
- 곡선 커버리지: 2호선 114/114, 4호선 96/96, 5호선 105/105 (이론상 최대치)
- 스파이크(>2km): 9호선급행 72 → **0** (급행 정규화), 8호선 별내선만 잔존

**결정 / 버린 대안:**
- 공식 철도중심선(KRIC/KTDB) 신청 버림 → 가입·승인 게이트, OSM으로 충분
- way-chaining은 보간/스무딩 아님 — 곡선 디테일은 이미 way 점에 있고 순서만 맞춤 → 실제 선로 그대로

**함정·깨진 것:**
- **8호선 별내선(2024.8 개통)이 KTDB GTFS(2024.3)에 없음** → 별내선 역(장자호수공원·다산·구리 등)이 7.9km 떨어진 암사로 스냅됨. 스냅 가드로 ODsay 폴백 처리(별내선 역 순서는 유지, 한강 횡단만 직선). **데이터 갱신 전까지 한계** (72 step, 0.5%, 구리/남양주발 원거리 경로)

**미완성/TODO:**
- 8호선 별내선: 더 최신 GTFS 또는 OSM 별내선 geometry 확보 시 해소
- 운영(Supabase) 실시간 경로 적용은 여전히 후속

---

## 2026-06-14 — GTFS 순서 + OSM 역쌍곡선 지하철 폴리라인 (Spec 31, Ralph 루프)

**의도:** 지하철 폴리라인의 분기 꼬임(5호선 등) 근본 해결. OSM "승차→하차 전체구간" 매칭이 분기에서 엉뚱한 갈래를 골라 꼬이던 문제를, KTDB GTFS의 방향·분기·권역별 분리된 정차 순서를 backbone으로 쓰고 **인접 역쌍마다** OSM 곡선을 이어붙여 해결.

**최종 상태:** 완료 (US-001~004 검증). API에서 5호선 ratio 18x→1.1, 모든 지하철 step 노선이탈 ≤0.39km, garbage 0건.

**건드린 파일·함수:**
- `scripts/migrate_gtfs_subway.sql` (신규): 5개 레퍼런스 테이블 DDL
- `scripts/build_gtfs_subway.py` (신규): GTFS routes/stops/stop_times 정제 → gtfs_subway_route(145)/station(1288)/seq(3865). stop_times 1.6GB는 RR_ + _Ord001 스트리밍 필터
- `scripts/reconcile_lines.py` (신규): GTFS↔OSM 계통 키 1:N 정합 → line_map. 수도권 S-1 미매칭 0개 (별칭: gtxa→a선, 공항철도→인천국제공항철도일반열차, 김포도시철도→김포골드라인, 우이신설경전철→우이신설선)
- `scripts/build_pair_geom.py` (신규): 인접 역쌍 OSM 곡선 → subway_pair_geom(1620). S-1 커버리지 99%(누락 17, 직선 fallback)
- `scripts/backfill_pair_geom.py` (신규): transit_routes 지하철 step 재생성. **시그니처(총시간+step type·노선) 매칭**으로 raw path↔transit_routes rank 정렬
- `app/gtfs_subway.py` (신규): build_subway_linestring — GTFS 역 스냅 → 순서대로 역쌍곡선 스티칭
- `app/subway_shapes.py`: _segment_from_candidates 추출 + get_segment_by_keys(line_map 키 기반) 추가. get_segment 동작 불변
- `app/detail.py`: routes 응답이 baked step_linestring 직접 반환 (런타임 enrich 제거)
- 테스트 부채 수리: conftest/test_search_pipeline/test_comments_endpoint 픽스처에 step*_linestring 컬럼 추가, test_transit step_cols 언패킹 6→7

**결정 / 버린 대안:**
- 위치(enumerate) 기반 backfill 버림 → fetch_cells가 filter/rank로 raw path를 재정렬·선별(24→18)하므로 misalignment. **시그니처 매칭**으로 교체 (이전 backfill_linestring.py의 잠재 버그도 회피)
- 누락 역쌍 곡선 복구 시도 버림 → OSM way 순서 불연속으로 16km garbage 우회로만 나옴(bbox가 옳게 차단). 17개는 직선 fallback
- region(부산/수도권) 구분은 line_map이 아니라 역 좌표(bbox)로 처리 → 동명 노선 자동 분리

**DB 변경:** scripts/migrate_gtfs_subway.sql (gtfs_subway_station/route/seq, subway_pair_geom, line_map). transit_routes step*_linestring 값 갱신(wp 3,7). 레퍼런스 테이블은 로컬 빌드용(멱등) — download_db.py 재다운로드 후 재실행 필요.

**함정·깨진 것:**
- transit_routes.rank ≠ raw JSON path 순서 (fetch_cells가 재정렬). 위치 기반 backfill은 노선-좌표 미스매치 야기 → 시그니처 매칭 필수
- **역쌍 곡선 내부 텔레포트**: OSM way 불연속으로 일부 역쌍(예 4호선 충무로→명동 곡선 2.25km/직선0.69km) 슬라이스 내부에 1.9km 점프 포함. bbox는 전체 범위만 봐서 못 거름 → build_pair_geom에 `_curve_ok`(상대 내부갭 + detour비율) 검증 추가, 불량 곡선은 직선 fallback. 결과: 4호선 ratio 2.4~2.9→1.2~1.5, 최대점프 1.91km→0.74km
- pytest: HEAD 베이스라인 32 failed → 내 브랜치 21 failed (회귀 0, linestring 부채 11개 수리). **남은 21개(test_search_pipeline 14 + test_dual_workplace 7)는 사전 존재 "0 cards" search 파이프라인 실패로 GTFS와 무관** (stash로 HEAD 재현 확인)
- 중복 wp_id 디렉토리(wp_15/16/17이 서로 다른 주소 2개씩) → 전체 backfill 시 미매칭 1241 (stale dir, 해당 행은 이전 값 유지)

**미완성/TODO:**
- 운영(Supabase) 실시간 partial-search 경로에 GTFS 스티칭 미적용 (baked 경로만). 레퍼런스 테이블 Supabase 반영은 후속
- 전체 wp backfill 미실행 (wp 3,7만). 나머지 wp는 `python scripts/backfill_pair_geom.py`로 일괄 가능
- **사전 존재 search 파이프라인 테스트 실패 21개** — 별도 조사 필요 (Spec 31 범위 밖)

---

## 2026-06-14 — OSM 지하철 선형 데이터 연동 + 폴리라인 렌더링 완성

**의도:** 카카오맵 위에 지하철 경로를 역-역 직선이 아닌 실제 선로 선형(OSM Overpass 데이터)으로 표시.

**최종 상태:** 완료.
- OSM Overpass API로 수도권 지하철 251개 노선 선형 다운로드 → `data/subway_shapes_kr.json` (11.5MB)
- `app/subway_shapes.py` 신규 — 노선명 정규화 + nearest-point 구간 추출 + `enrich_linestring()`
- `app/detail.py` — `/api/apt/{seq}/routes` 응답 시 지하철 linestring을 OSM 고품질로 교체
- `web/result.html` — route-overlay 투명화(지도 클릭 차단 제거), `closeRoute()`에서 `clearPolylines()` 제거(닫아도 선 유지), `showDetail()`에서 폴리라인 자동 그리기, `strokeStyle` `'dashed'`→`'shortdash'` 수정
- 실 API 테스트: 수도권 2호선 서초→삼성 4pts → 10pts (OSM enrich 확인)

**건드린 파일·함수:**
- `app/subway_shapes.py` (신규): `enrich_linestring()`, `get_segment()`, `_norm()`, `_load_index()`
- `app/detail.py`: routes 응답 빌드 부분 — `enrich_linestring()` 호출
- `web/result.html`: `closeRoute()`, `showDetail()`, `.route-overlay` CSS, `drawRoutePolylines()`
- `scripts/download_subway_shapes.py` (신규): Overpass API 쿼리 + 수도권 필터
- `data/subway_shapes_kr.json` (신규): 11.5MB, 251개 노선

**결정 / 버린 대안:**
- ODsay passShape 대신 OSM: ODsay 무료 플랜은 passShape 제공 안 함
- GTFS shapes.txt 대신 OSM: KTDB는 담당자 신청 방식, OSM은 즉시 무료 사용 가능
- 버스 선형은 현재 방식(정류장 좌표 직선) 유지: 버스 노선 데이터는 별도 API 필요, 범위 밖

**DB 변경:** 없음 (이전 세션에서 migrate_add_linestring.sql 이미 적용)

**함정·깨진 것:**
- `strokeStyle: 'dashed'` → Kakao Maps 미지원, `'shortdash'`로 수정
- `_STRIP_PREFIX`에 `수도권` 단독 미포함으로 "수도권 5호선" 정규화 실패 → 정규식에 추가
- `route-overlay` 배경색이 지도를 가리고 있었음 → `background: transparent; pointer-events: none`
- OSM 타 도시 동명 노선(부산 2호선 등) 혼입 → score(km) 기준으로 자동 필터링됨

**미완성/TODO:**
- 버스 노선도 실제 선형으로 교체하려면 공공데이터포털 버스 노선 API 필요

---

## 2026-06-14 후속 — wp_id=3,7 ODsay 재수집 + linestring 백필

**의도:** 이전 세션에서 transit.py linestring 추출 코드 추가 전에 수집된 wp_id=3(용산구 한강대로 32), wp_id=7(강남구 도산대로 139)의 raw JSON이 없어 pts=0이던 문제 해결.

**최종 상태:** 완료.
- wp_id=3: 898셀 재수집 → 898 passed, 0 failed (56s)
- wp_id=7: 1159셀 재수집 → 1159 passed, 0 failed (67s)
- `backfill_linestring.py --apply` → 35,984행 업데이트 (OSM linestring 포함)
- API 검증: `/api/apt/11140-15/routes?wp_id=3` → 지하철 4호선 86pts, wp_id=7 → 3호선 235pts 확인

**건드린 파일·함수:**
- `scripts/backfill_linestring.py`: 수정 없음 (기존 스크립트 재실행)
- `.tmp/recollect_linestring.py`: 임시 재수집 스크립트 (git 미추적)

**DB 변경:** transit_routes step1~5_linestring 업데이트 (35,984행)

**함정·깨진 것:**
- step1(도보), step3, step5는 NULL로 남음 → 도보 구간 좌표가 없는 경우 정상
- step2(지하철/버스)에 linestring 채워짐으로 폴리라인 표시 정상 작동
