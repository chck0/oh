## 2026-06-19 07:47 — 경로 패널 네이버형 리디자인 + transit_routes 형상 dedup + 빌드DB 분리

**의도:**
- 경로 정보 UI를 네이버처럼 "한 줄 여정 요약 바 → 클릭 시 상세 타임라인"으로 재설계 (이모지 제거, 지하철 공식 노선색 반영).
- 로컬 `apartment.db`가 992 MB로 비대 → `transit_routes` 형상(linestring) 중복(4.1배)·빌드 중간 테이블 정리.
- 운영 Supabase에도 좋은 형상을 올리되 무료 한도(500 MB)를 지키기 위한 사전 정지작업(어댑티브 리더).

**최종 상태:**
- `web/result.html`: `routeCardHTML` 단일 렌더러로 통일(하단시트·상세패널 공용). 요약 바 = `도보 (3분)` / `2호선 (18분)` / `504 (12분)` 글자라벨 + 시간비율 폭. 펼침 = 세로 타임라인(색점 + 노선배지 + 승차역→하차역). `_subwayColor()`(1~9호선·경의중앙·신분당·수인분당·공항철도·신림선·GTX-A 등 공식색) + `_textOn()`(명도 기반 글자색). 버스는 임시 테마색. 지도 핀의 상시 역명 라벨은 제거(패널이 대체), 승·하차 점만 유지.
- `app/detail.py`: `/routes` 리더를 **스키마 자동감지**로. `step1_geom_id` 존재 시 `route_geom` JOIN으로 형상 복원(COALESCE 폴백), 없으면 기존 컬럼. 컬럼 순서 동일 → 파싱 로직 불변.
- 로컬 DB: dedup(992.8→562.6 MB) + 빌드테이블 분리(562.6→300.8 MB). 빌드 산출물은 `data/geom_build.db`(261.5 MB)로 이관.

**건드린 파일·함수:**
- `web/result.html` — `routeCardHTML`/`_subwayColor`/`_textOn`/`_segLabel`/`_segColor`/`_rtToggle` 신규, `_routeOptionsHtml`·`renderRoutesHTML` 교체, 옛 `renderSingleRoute`/`renderStepHTML`/`toggleRoute`/`routeModeLabel` 제거. dp-bar/dp-seg/dp-tl-* CSS 추가. `drawRoutePolylines` 핀 라벨 제거.
- `app/detail.py` — `_routes_query(conn)` + `_DEDUP_SCHEMA` 감지 추가, `apt_routes` 쿼리 교체.
- `scripts/dedup_route_geom.py`(신규) — route_geom 적재 + geom_id 매핑 + linestring NULL + VACUUM (로컬 SQLite 전용, 멱등 가드).
- `scripts/split_geom_build.py`(신규) — 빌드 중간테이블 10개를 geom_build.db로 이관(카운트 검증 후 DROP+VACUUM).

**결정 / 버린 대안:**
- 쓰기 경로(`transit.py collect`)는 그대로 두고 읽기만 dedup → 라이브 INSERT 무위험. 신규 행은 linestring으로 들어오고 리더가 COALESCE 폴백.
- 운영 재적재 방식: 좋은형상 그대로 = ~513 MB(한도 초과) → **dedup 필수** 확정(압축비 0.742 실측). 좌표 5자리 절삭은 9%뿐이라 기각.
- 빌드테이블은 삭제 아닌 **분리 보관**(ODsay 재호출 금지라 재베이크 원천 보존).

**DB 변경:**
- 로컬: `route_geom(id, ls)` 테이블 + `transit_routes.step1~5_geom_id` 컬럼 추가. 빌드테이블 10개 `geom_build.db`로 이관(운영엔 원래 없음).
- 스키마 변경 스크립트: `scripts/dedup_route_geom.py`, `scripts/split_geom_build.py`.
- 운영 Supabase: **아직 미적용**(아래 TODO).

**함정·깨진 것:**
- Postgres는 형상을 TOAST 압축 → 운영은 이미 335 MB(로컬 992는 SQLite 비압축 탓). 좌표 텍스트는 압축비 0.742로 잘 안 줄어듦.
- 운영 `transit_routes`는 로컬보다 형상이 **stale**(샘플 197/200 상이, 운영 3248자 vs 로컬 5419자) — 이번 loadLane 개선이 운영 미반영.
- 운영 추가셀 1,103쌍은 형상 전부 NULL(백필 안 된 옛 셀) — 로컬에 없어 동기화로 못 고침(현재도 폴리라인 안 나옴, 퇴보는 없음).
- dedup 적용 전 어댑티브 리더 미배포 시 구 리더가 NULL linestring 읽어 운영 폴리라인 깨짐 → **반드시 배포 선행**.

**미완성/TODO:**
- 운영 Supabase 마이그레이션: (1) `detail.py` 배포 → (2) 운영 백업 → (3) 5432 세션에서 원자적 TRUNCATE+적재(로컬 좋은형상 dedup + 운영추가셀 보존) → (4) 검증 게이트 통과 시 COMMIT → (5) `/routes` 실호출 확인. 예상 최종 ~258 MB.
- 버스 고유색(종류별) 미정 — 임시 테마색.
- 운영 추가셀 1,103개 형상 백필(line_geom 클립)은 별도.
