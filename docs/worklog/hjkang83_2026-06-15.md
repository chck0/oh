# 작업 일지 — hjkang83 (2026-06-15)

최신 작업이 맨 위.

---

## 2026-06-15 06:35 — 선택 단지 핀 강조 (지도에서 식별) · spec-38

**의도:** 지도에서 아파트 핀/카드를 클릭하면 ① 지도가 이동(panTo)하고 ② 주변 POI 핀이 찍히는데, 정작 **선택한 단지 핀 자체는 주변 핀과 똑같이 보여** "지금 보는 단지가 어느 핀이지?"를 한눈에 알 수 없었음 → 선택 핀을 시각적으로 강조.

**최종 상태:** 핀/카드 클릭으로 상세를 열면 해당 단지 핀을 **확대(scale 1.18~1.26) + 흰색 외곽 링 + 글로우 + 은은한 펄스 + 최상단(zIndex 200)** 으로 강조. 단지 전환 시 이전 강조 해제, 패널 닫기 시 해제. 항상 1개만. 전체 410 passed. PR #134 스쿼시 머지(`dd5acc5`).

**건드린 파일:**
- `web/result.html` — CSS `.pin.pin-selected`(+`@keyframes pinSelPulse`), JS `setActiveAptPin`/`clearActiveAptPin`, 호출 연결(`openDetail`·`showDetail` set / `closeDetail`·`backToList` clear)
- `docs/specs/38-selected-pin-highlight.md` — 신규(Implemented)

**결정 / 버린 대안:**
- 흰 외곽 링: `drop-shadow`를 상하좌우 4방향으로 겹쳐 집 모양(지붕 삼각형 포함) 실루엣을 따라 흰 테두리 생성 → CSS만으로 불규칙 도형에 링 효과
- ❌ 핀 색상 자체 변경: 통근(t1~t4)·추천(보라) 색 체계가 정보를 담고 있어 덮으면 안 됨 → 색은 유지하고 강조만 덧입힘
- 선택자 이스케이프 회피: `.pin[data-apt-seq]` 순회 비교(apt_seq에 하이픈 등 포함 가능)
- 오버레이 `setZIndex(200)` + 이전값 원복으로 항상 최상단

**DB 변경:** 없음 · **API 변경:** 없음 (순수 프론트)

**함정·깨진 것:** 샌드박스는 Kakao SDK 차단으로 지도 카드 렌더 중단 → 핀 강조 시각검증 불가(코드 로직으로 검증). 실제 화면에서 링/펄스 강도 확인 필요.

**미완성/TODO:** 실기기에서 강조 강도(링 두께·펄스) 적절성 확인 후 필요 시 조정.

---

## 2026-06-15 — main 재동기화 (PR #133·#134 머지 후)

**의도:** main에 머지된 PR들(#133 단지 비교표 항목 교체, #134 선택 핀 강조)을 hjkang83에 반영해 두 브랜치 트리를 일치시킴.

**최종 상태:** 비파괴 방식(`merge -s ours` + `read-tree --reset origin/main`)으로 hjkang83 트리를 main과 동일하게 스냅. `git diff origin/main origin/hjkang83` 빈 출력 확인. 히스토리 보존(force-push 없음).

**건드린 파일:** 없음 (머지 커밋만)

**DB/API 변경:** 없음
