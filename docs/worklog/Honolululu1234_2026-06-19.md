## 2026-06-19 — main 결과 화면 전환 버그 수정

**의도:** Vercel main 결과 화면에서 로딩 완료 후 지도가 비어 보이고 카드가 렌더링되지 않는 문제 수정.
**최종 상태:** YouTube 로딩 영상 전환 후 남아 있던 삭제된 MP4 엘리먼트 호출을 제거. 결과 보기 클릭 시 `renderSummary`/`renderCards`/`initMap`까지 정상 진행되도록 복구.
**건드린 파일·함수:** `web/result.html` — `showResults`
**추가 보정:** `app/detail.py` — 테스트 DB에 `apartments.kaptAddr` 컬럼이 없는 경우에도 상세 API가 동작하도록 컬럼 존재 여부를 확인해 빈 주소로 fallback.
**DB 변경:** 없음
**함정·깨진 것:** `main`의 로딩 UI는 YouTube iframe으로 바뀌었지만 기존 `vCurr`/`vNext` pause 호출이 남아 JS 런타임 에러를 만들 수 있었음.
**미완성/TODO:** 없음
