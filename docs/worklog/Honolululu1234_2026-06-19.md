## 2026-06-19 00:59 — 서버리스 PoolTimeout 수정

**의도:** Vercel 배포 시 `/api/search` 에서 `PoolTimeout: couldn't get a connection after 10.00 sec` 에러 발생 → 서버리스 환경에서 커넥션 풀 비활성화
**최종 상태:** `IS_SERVERLESS=True`일 때 `_get_pool()`이 즉시 `None`을 반환 → 직결(psycopg.connect)로 폴백
**건드린 파일·함수:** `app/db.py` — `_get_pool()`
**결정 / 버린 대안:** 풀 `open=False`로만 바꾸는 방법도 있었으나, 서버리스에서 풀 자체가 불필요하므로 완전히 우회하는 방향 선택
**DB 변경:** 없음
**함정·깨진 것:** 없음 (로컬 개발은 IS_SERVERLESS=False라 기존 풀 그대로 사용)
**미완성/TODO:** Supabase 커넥션 수 모니터링 권장
