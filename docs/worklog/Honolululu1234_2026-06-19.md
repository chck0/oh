## 2026-06-19 09:00 — 카드 컬럼 최적화: 너비 360px, X 닫기/목록 열기 토글, 개별 카드 렌더링 활성화

**의도:** 카드 컬럼 너비 300→360px로 아파트명 잘림 해소. X 버튼으로 컬럼 숨기기/지도 왼쪽 "목록" 버튼으로 다시 열기. 비활성화된 개별 카드 렌더링 재활성화.
**최종 상태:** 360px 카드 컬럼에 추천+나머지 카드 목록 표시. X 클릭 시 collapsed 애니메이션 후 지도 전체폭 확장. 지도 좌측 "목록" 세로버튼으로 복원.
**건드린 파일·함수:** `web/result.html` — `.card-column` CSS(300→360px, collapsed/transition), `.card-column-close/open-btn` CSS 신규, `toggleCardColumn()` JS 신규, HTML(닫기버튼+열기버튼 추가), `renderCards()` JS(`if(false &&...)`→평면 카드 리스트로 교체)
**결정 / 버린 대안:** 버킷 그룹(통근시간대별 접이식) 방식 제거→추천+나머지 단순 평면 리스트로 교체. 닫기 후 복원 버튼을 left-panel 대신 map-panel 왼쪽 가장자리에 배치.
**DB 변경:** 없음
**함정·깨진 것:** 없음
**미완성/TODO:** 없음

## 2026-06-19 07:30 — 3패널 레이아웃: 정렬기준 왼쪽 패널 인라인 + 매물카드 컬럼 분리

**의도:** 정렬 기준 드롭다운이 오른쪽 지도/카드 위에 floating으로 뜨는 문제 수정 → 왼쪽 패널 인라인으로 이동. 매물 카드도 별도 중간 컬럼으로 분리.
**최종 상태:** left-panel(280px, 필터+정렬기준 인라인) | card-column(320px, 매물카드) | map(flex:1) 3컬럼 레이아웃. 정렬 기준 패널 클릭 시 패널 내부에서 펼침.
**건드린 파일·함수:** `web/result.html` — `.left-panel` CSS(280px), `.card-column` CSS 신규, `.sort-panel-body` CSS(fixed→static), HTML 구조(sort-panel-body를 sort-panel 내부로 이동, cards-panel을 card-column으로 이동), `toggleSortPanel()` JS(동적 top 계산 제거)
**결정 / 버린 대안:** `position: fixed; left: 448px` floating 방식에서 `position: static` 인라인 방식으로 전환. 이전 body 직하위 sort-panel-body DOM 노드 제거.
**DB 변경:** 없음
**함정·깨진 것:** sort-panel-body가 body 직하위와 sort-panel 내부 두 곳에 있어 중복 제거 필요했음.
**미완성/TODO:** 없음

## 2026-06-19 06:00 — 평형 필터 히스토그램+범위슬라이더로 교체

**의도:** 상세조건의 평형 필터를 기존 칩 방식에서 준공연도·세대수와 동일한 히스토그램+듀얼핸들 범위슬라이더 형태로 교체
**최종 상태:** 평형 히스토그램(#A82E1B), 듀얼썸 슬라이더 정상 렌더링. 0~50평+ 6개 버킷(pyeong_type 매핑). 슬라이더 드래그로 카드·지도핀 동시 필터링.
**건드린 파일·함수:** `web/result.html` — adv-pyeong-histogram/track/fill/thumb HTML, `_pyeongSlider`, `_PYEONG_BUCKETS`, `_PYEONG_MAP`, `_initPyeongSlider`, `_renderPyeongSlider`, `_initPyeongSliderDrag`, `_advFilter`, `resetAdvFilter`, `_isAdvFilterActive`, `_filterCardsByAdv`, `renderCards`
**결정 / 버린 대안:** 칩 방식(`toggleAdvChip`으로 pyeong 배열)에서 연속 범위 슬라이더로 전환. pyeong_type 문자열을 숫자 버킷으로 매핑(_PYEONG_MAP).
**DB 변경:** 없음
**함정·깨진 것:** `_advFilter.pyeong` 배열 제거 → `pyeongMin/pyeongMax`로 교체. `_isAdvFilterActive` 조건 업데이트 필요.
**미완성/TODO:** 없음

## 2026-06-19 04:20 — 정렬 기준 드롭다운 지도 영역 오버레이로 이동

**의도:** 정렬 기준 드롭다운이 카드 목록을 가려 매물이 안 보이는 문제 수정 — 지도 영역 위에 floating 오버레이로 표시
**최종 상태:** `sort-panel-body`를 `<body>` 직하위로 이동 + `position: fixed; left: 448px` + JS로 top 동적 계산 → 카드와 드롭다운 동시 표시
**건드린 파일·함수:** `web/result.html` — `.sort-panel-body` CSS, `sort-panel-body` HTML 위치, `toggleSortPanel()` JS
**결정 / 버린 대안:** `position: absolute; overflow: visible`로 패널에서 넘치는 방식도 시도했으나 left-panel overflow:hidden으로 불가. `position: fixed`를 body 직하위에서 사용하는 방식으로 해결.
**DB 변경:** 없음
**함정·깨진 것:** 브라우저가 이미 로드된 HTML을 캐싱해 리로드 전까지 position:absolute로 계산됨. viewportW=2048, screenshot은 1568px 물리픽셀(0.766배 축소)임을 확인.
**미완성/TODO:** 없음

## 2026-06-19 03:10 — 정렬 기준 드롭다운 위로 펼침 + 마트 도보 가시성 수정

**의도:** 정렬 기준 드롭다운이 화면 하단에서 잘려 "마트 도보" 항목이 보이지 않는 문제 수정
**최종 상태:** 드롭다운이 아래(`top:100%`) 대신 위(`bottom:100%`)로 펼쳐져 모든 항목 표시
**건드린 파일·함수:** `web/result.html` — `.sort-panel-body` CSS (`top→bottom`, `border-top→border-bottom`, `border-radius`, `box-shadow`, `transform-origin`)
**결정 / 버린 대안:** `overflow-y:auto` + 고정 높이 방식도 가능했으나 스크롤 UX가 떨어지므로 위로 펼치는 방식 선택
**DB 변경:** 없음
**함정·깨진 것:** 없음
**미완성/TODO:** 없음

## 2026-06-19 01:48 — 상세조건 필터 + 히스토그램 슬라이더 추가

**의도:** 결과 화면 좌측 패널에 상세조건 필터(평형·준공연도·세대수) 추가. 준공연도·세대수는 히스토그램 + 범위 슬라이더 형태로 구현. 필터 적용 시 카드 목록과 지도 핀 동시 갱신.
**최종 상태:** 평형(복수 선택 칩), 준공연도(히스토그램 슬라이더), 세대수(히스토그램 슬라이더) 모두 정상 동작. BADUGI 메인 색상(#A82E1B) 적용. filterMapPins 연동.
**건드린 파일·함수:** `web/result.html` — adv-filter-panel HTML, CSS, `_initYearSlider`, `_renderYearSlider`, `_initUnitsSlider`, `_renderUnitsSlider`, `_applyAdvFilter`, `_filterCardsByAdv`, `renderCards`
**결정 / 버린 대안:** 세대수 50세대 단위 버킷으로 히스토그램 구성. 칩 방식(300세대+, 500세대+...)은 제거.
**DB 변경:** 없음
**함정·깨진 것:** `kaptUsedate` 필드가 카드에 없어 `build_year`로 교체. `allCards` 중복 선언 버그 수정.
**미완성/TODO:** 없음

## 2026-06-19 00:59 — 서버리스 PoolTimeout 수정

**의도:** Vercel 배포 시 `/api/search` 에서 `PoolTimeout: couldn't get a connection after 10.00 sec` 에러 발생 → 서버리스 환경에서 커넥션 풀 비활성화
**최종 상태:** `IS_SERVERLESS=True`일 때 `_get_pool()`이 즉시 `None`을 반환 → 직결(psycopg.connect)로 폴백
**건드린 파일·함수:** `app/db.py` — `_get_pool()`
**결정 / 버린 대안:** 풀 `open=False`로만 바꾸는 방법도 있었으나, 서버리스에서 풀 자체가 불필요하므로 완전히 우회하는 방향 선택
**DB 변경:** 없음
**함정·깨진 것:** 없음 (로컬 개발은 IS_SERVERLESS=False라 기존 풀 그대로 사용)
**미완성/TODO:** Supabase 커넥션 수 모니터링 권장
