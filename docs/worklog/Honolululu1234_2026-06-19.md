## 2026-06-19 21:02 — route 폴리라인 선 디자인만 main 브랜치에 이식

**의도:** `route` 작업 중 마지막으로 조정한 지도 경로선 디자인만 최신 `main` 기반 새 브랜치에 반영. 경로 카드/랜딩/정렬/상세 패널 UI 변경은 제외.
**최종 상태:** 교통 경로선은 검은 외곽 대신 흰 케이싱 + 얇은 노선색 본선으로 표시. 버스는 표준 파랑, 알 수 없는 지하철은 회색 fallback. 도보 연결선은 회색 점선으로 축소. 승하차 핀 라벨은 다시 표시.
**건드린 파일·함수:** `web/result.html` — `_renderRouteLines()`, `drawRoutePolylines()`
**결정 / 버린 대안:** `route` 전체 cherry-pick/merge는 다른 UI 변경이 섞여 제외. stash에 남아 있던 최종 선 디자인 값만 수동 반영.
**DB 변경:** 없음
**함정·깨진 것:** 최신 `main`에도 폴리라인 렌더링 로직 자체는 이미 있었지만, 마지막 선 색상·두께 조정은 stash 상태에만 남아 있었음.
**미완성/TODO:** 없음
