# 작업 일지 — hjkang83 (2026-06-13)

최신 작업이 맨 위.

---

## 2026-06-13 16:30 — spec-36 대출 가능액 역산 계산기 구현

**의도:** 검색 "최대 가격"을 직접 숫자로 넣는 대신, 보유 현금+연소득으로 매수가능액을 역산해 채워주는 보조 위젯. 사용자 결정: ① 현행 법규 기반 ② 별도 섹션 ③ 최대만 채움.

**최종 상태:** search.html에 접이식 독립 섹션 구현. 비규제 LTV70%·규제 LTV50%+절대캡(6/4/2억)·스트레스 DSR(40%+1.5%p)·현행 금리 4.5% 기본. LTV/캡/DSR 중 최소를 역산 → "이 금액을 최대 가격으로" 버튼으로 price 칸 주입. 라이브 검증: 현금1.5억+소득5천 → 비규제 4.2억(DSR제약), 규제 3억(LTV제약). 전체 406 passed(백엔드 무변).

**건드린 파일:**
- `web/search.html` — 신규 계산기 섹션 HTML + CSS + JS(`AFFORD` 상수, `calcAffordability`, 토글/자동재계산/채우기)
- `docs/specs/36-affordability-calculator.md` — 현행 법규 기반으로 갱신 + Approved + AC 체크

**결정 / 버린 대안:**
- 단계 번호(1~5) 안 흐트러지게 계산기는 번호 없는 보조 섹션으로 가격 범위 위에 배치
- 규제지역 LTV 50%·절대캡은 보수적 가정값(정확값은 주택가·주택수·생애최초별 상이) → "추정·은행 확인" 병기
- DSR은 스트레스 가산(+1.5%p) 심사금리로 계산 (3단계 시행 반영)
- 입력 변경 시 자동 재계산(별도 계산 버튼 없음)
- 매니페스토 정합성: "예산 모르는 초보 제외" → 사전심사 아닌 보조 위젯으로 한정, 단정 금지

**DB 변경:** 없음 · **API 변경:** 없음 (기존 max_price 흐름 재사용)

**함정·깨진 것:**
- 규제지역 절대 대출캡은 집값 구간에 의존(6/4/2억) → 구간별 피스와이즈로 최대 집값 산출(`_regulatedMax`).
- 현금 0이면 LTV상 매수가 0이 됨 → 현금 0일 땐 LTV 제약 스킵하고 "대출만 가정" 안내.
- `--color-primary` 미정의 → `--color-brand-primary` 사용.

**미완성/TODO:**
- 규제·금리는 수시 변동 → `AFFORD` 상수는 2026-06 스냅샷. 주기적 현행화 필요.
- 다주택자 LTV 차등·기존대출 차감(DSR 잔여)·정책상품(보금자리 등)은 범위 외.

## 2026-06-13 15:40 — spec 번호 중복(20·28) 정리 + worklog 도입

**의도:** main에 spec 번호가 20·28에서 각각 2개씩 중복돼 있었음. worklog도 안 남기고 있었음 → 둘 다 정리.

**최종 상태:**
- 중복 해소: 나중에 들어온 spec을 빈 번호로 재배정.
  - `20-multi-priority-sort` → **34** (원본 20은 transit-route-detail 유지)
  - `28-search-refactor` → **35** (원본 28은 commute-economics 유지)
- 생성 시점(git log --diff-filter=A)으로 원본/중복 판별: 원본이 먼저, 재배정 대상이 나중.
- AGENTS.md "다음 신규 Spec 번호" 34 → **36**, 완료 목록 갱신.
- 이 worklog 파일 신설.

**건드린 파일:**
- `docs/specs/20-multi-priority-sort.md` → `34-multi-priority-sort.md` (헤더 타이틀 34로, 재배정 주석)
- `docs/specs/28-search-refactor.md` → `35-search-refactor.md` (헤더 타이틀 35로, 재배정 주석)
- `web/result.html` — 정렬 코드 주석 `(spec-20)` → `(spec-34, 구 spec-20)`
- `AGENTS.md` — Spec 번호 현황 갱신
- `docs/worklog/hjkang83_2026-06-13.md` — 신설

**결정 / 버린 대안:**
- ❌ 원본(먼저 만든 spec)을 옮기기: git 히스토리·다른 분 참조가 더 많아 혼란 큼
- ✅ 나중 것을 옮김 + "구 spec-NN" 주석으로 추적성 유지
- spec-20의 다른 참조(detail.py·patent·whytree의 "경로 상세 팝업")는 **원본 transit-route-detail**을 가리키므로 그대로 둠. 정렬을 가리키는 result.html 주석 1곳만 34로 수정.

**DB 변경:** 없음

**함정·깨진 것:**
- `spec-20`/`spec-28` 문자열이 두 의미(원본/중복)로 섞여 있어, 일괄 치환하면 안 됨 → 의미별로 가려서 result.html 1곳만 수정.

**미완성/TODO:** 없음

---

## 2026-06-13 15:30 — spec-31 용적률·건폐율 0% 오표시 수정 (#122)

**의도:** 운영에서 일부 단지(쌍용더플래티넘용마산)가 단지 상세 "입지·구조"에 용적률 0%·건폐율 0%로 뜸. 실제 아파트가 0%일 수 없으니 명백한 버그.

**최종 상태:** 건축물대장(`building_register`) `vlRat`/`bcRat`가 0(미집계)인 경우 행을 숨김. 라이브로 0값 주입 후 `far`/`bcr` 미노출 + 구조·경사는 유지 확인. 전체 405 passed.

**건드린 파일:**
- `app/detail.py` — 집계에서 `None`뿐 아니라 `> 0` 만 채택 (0/음수는 미집계로 간주)
- `web/result.html` — 프론트 방어적으로 `b.far > 0`/`b.bcr > 0` 조건 추가
- `tests/test_detail_endpoint.py` — 0값 → 행 숨김 + 유효 데이터 유지 검증 추가
- `docs/specs/31-slope-rebuild-indicators.md` — Edge Case 문구를 "행 숨김"으로 확정

**결정 / 버린 대안:**
- ❌ 프론트에서만 0 거르기: 백엔드가 여전히 0을 내보냄 → 데이터 책임은 백엔드에 둠
- ✅ 백엔드 집계에서 0 제외(주 수정) + 프론트 방어(이중 안전)

**DB 변경:** 없음 (조회 로직만)

**함정·깨진 것:**
- `_far_level(0)`이 None을 반환하는데도 프론트는 `b.far != null`이라 0을 렌더 → "0%"만 떴음. null 체크만으론 부족, `> 0` 필요.

**미완성/TODO:** 없음

---

## 2026-06-13 15:00 — main 병합 + 테스트 하네스 버그 수정 (#121 후속)

**의도:** spec-31/32 작업 브랜치에 main을 병합해 최신화. 병합 후 검색·dual 테스트가 대량 실패하는 원인을 잡음.

**최종 상태:** main 병합(search.py 분할 → detail.py로 spec-31 로직 이식) + 테스트 하네스 2종 + 의존성 1종 수정. 전체 **404 passed → 405 passed**(이전 40건 실패 → 0).

**건드린 파일:**
- `app/detail.py` — spec-31 입지·구조 로직을 `_q_infra` 병렬 쿼리로 이식 (search.py가 cards/chat/detail.py로 분할됨)
- `app/workplaces.py` — 데모 게이트를 main의 캐시/DB 조회 계층 앞단에 재배치 + `import os`
- `tests/test_search_pipeline.py` — `_FULL_SCHEMA`에 `apt_walking_poi` 추가 (누락 시 검색 500)
- `app/main.py` + `tests/conftest.py` — 시작 캐시 워밍을 `BADUGI_NO_WARM`으로 게이트(테스트 레이스 제거)
- `requirements.txt` — `aiohttp<3.14` 핀 (aioresponses 0.7.8 비호환)

**결정 / 버린 대안:**
- search.py는 main 버전 채택(분할 결과), spec-31 코드만 detail.py로 옮김
- spec 번호 충돌: 데모 30→32, 실거래 29→33 (slope는 31 유지)

**DB 변경:** 없음

**함정·깨진 것:**
- main의 spec-34(정렬, 구 20)이 `apt_walking_poi`를 **가드 없이** 조회하는데 픽스처에 테이블이 없어 35건 500 연쇄. 픽스처에 테이블 추가로 해결.
- 시작 워밍(`_warm_caches`)이 라우팅 안 된 실 `db_connect()`로 모듈 캐시를 채워 테스트를 비동기로 재오염 → 게이트로 차단.

**미완성/TODO:**
- `app/search.py`의 `apt_walking_poi` 조회는 여전히 try/except 가드 없음 → 운영 DB에 테이블 비거나 없으면 500 위험. graceful 가드 별도 검토.

---

## 2026-06-13 14:00 — spec-31 경사도·재건축/구조 지표 + spec-32 로컬 데모 모드 (#121)

**의도:** 실구매자에게 필요한데 빠진 기능 보강. ① 이미 DB에 수집됐지만 화면에서 안 쓰던 경사도·용적률/건폐율을 노출. ② API 키 없이 전체 플로우를 띄우는 로컬 데모.

**최종 상태:**
- **spec-31**: 단지 상세 모달에 "입지·구조" 섹션 — 경사(체감 라벨+한 줄 설명+CSS 인디케이터, 도°는 툴팁), 용적률/건폐율(낮은편/보통/높은편 중립 힌트), 구조, 사용승인일.
- **spec-32**: `python scripts/run_demo.py` 한 번으로 강남역 기준 가상 단지 10곳 시드 + 서버 기동. `BADUGI_DEMO=1` 게이트로 시드 직장만 Kakao 없이 반환(운영 무영향).

**건드린 파일:**
- `app/detail.py` — `_slope_label`/`_far_level`/`_bcr_level`/`_approve_ym` + 입지·구조 조회·집계
- `web/result.html` — 입지·구조 섹션 + `.slope-ind` CSS
- `app/workplaces.py` — 데모 게이트
- `scripts/seed_demo_data.py`, `scripts/run_demo.py`, `scripts/run_demo.sh` — 데모 시드/런처
- `tests/test_detail_endpoint.py`, `tests/test_workplaces.py` — 단위·통합·graceful 테스트

**결정 / 버린 대안:**
- 경사 도(°)는 본문에 안 띄우고 체감 언어로 번역(라벨+한 줄), 원본은 툴팁
- 용적률/건폐율은 "재건축 유망" 같은 단정 금지 → 일반 밴드 대비 위치만 (매니페스토 준수)
- 경사 레벨(1~4)은 백엔드 산출 → 프론트가 라벨 문자열에 결합되지 않게
- `building_register` 동별 다행 → Python 집계(AVG/최빈값/MIN), 양 DB 호환
- 두 신규 테이블 각각 try/except + rollback (trade_tags 함정 패턴)

**DB 변경:** 없음 (기존 `apt_slope`·`building_register` 읽기만). 데모는 로컬 SQLite에만 시드.

**함정·깨진 것:**
- `apt_slope`/`building_register`는 수집 스크립트는 있었으나 런타임 코드에서 한 번도 안 읽고 있었음 → 노출만 추가.

**미완성/TODO:**
- 카드(목록)에 경사 칩 노출은 v2로 보류 (상세에서 먼저 검증).
- `apt_slope_avg` 단위(도°/% 구배) 프로덕션 데이터로 확인 후 임계값 보정.
