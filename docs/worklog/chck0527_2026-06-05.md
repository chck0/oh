# chck0527 작업 일지 — 2026-06-05

## 2026-06-05 — POI 지도 마커 복구 + 교통 라벨

**의도:** 좌측 POI 클릭 시 지도 마커가 안 찍히는 문제 해결, 교통 항목 가독성 개선.
**최종 상태:** 완료 (마커 동작 사용자 확인). 
**건드린 파일:** `app/main.py`(kakao-sdk 로더), `web/result.html`(showPoiMarker, POI 라벨).
**핵심:**
- 원인: `/api/kakao-sdk` 302 리다이렉트 → DOM `<script>` src가 프록시 URL이라 Kakao SDK가
  `libraries=services`를 못 읽어 `kakao.maps.services` 미로드 → 마커/POI필터 throw.
  → 302 대신 **실제 dapi `<script>`를 document.write로 주입하는 JS 로더**로 변경 (키는 서버 주입 유지).
- `showPoiMarker`에 substring 폴백 + `sort:DISTANCE` 추가 (축약 시설명 대응).
- 교통(D) 항목 표시명에 `(지하철역)`(D01)/`(버스정류장)`(D02) 라벨 — 검색용 data-poi-name은 원본 유지.
**함정:** 일부 단지(예 41310-60 구리시) `apartments.lat/lng`가 ~20km 어긋남 → 좌표 데이터 품질 이슈(별도, 추후 재지오코딩 검토).
**DB 변경:** 없음.

## 2026-06-05 — 초등학교 도보 POI 적재 (Spec 29)

**의도:** apt_walking_poi에 중·고만 있고 초등학교가 빠져 있어 NEIS로 보강.
**최종 상태:** 완료. 운영 Supabase에 초등 30,430건 적재.
**건드린 파일:** `scripts/fetch_elementary_poi.py`(신규), `config.py`(NEIS_API_KEY), `.env`/`.env.example`, `web/result.html`(라벨 주석).
**핵심:**
- NEIS(서울/경기/인천 2,272개) → Kakao 지오코딩(2,264) → 단지 도보15분(1100m) haversine → INSERT.
- 전용 서브코드 **A04** 사용 (A01=중/A02=고/A03=대 이미 점유). 이름 무관 멱등 DELETE 달성.
- 화면은 lclas='A'(="학교")로 묶여 초·중·고 함께 표시.
**DB 변경:** `apt_walking_poi`에 데이터 INSERT (스키마 변경 없음). 멱등·되돌리기(DELETE A04) 가능.

## 2026-06-05 — search.py 모듈 분할 (Spec 28)

**의도:** `app/search.py` 1592줄 비대 → 책임별 분할.
**최종 상태:** 완료. 1592 → 755줄. 388 테스트 통과, 동작 변경 0.
**건드린 파일:** `app/search.py`, 신규 `app/chat.py`·`app/detail.py`·`app/cards.py`, `tests/conftest.py`.
**핵심:** 각 모듈 자체 APIRouter → search.py에서 include_router. main.py 무변경.
conftest의 db_connect 몽키패치를 app.detail에도 적용.

## 2026-06-05 — 잡다한 수정·정리

- **검색 조건 UX:** 가격 최대 입력 시 최소값 자동(=최대−5억) 채움 (`web/search.html`).
  통근 10분 옵션은 여유분 차감 예외(그대로 10분), `COMMUTE_BUFFER_MIN` 15→10 복구 (`config.py`).
- **호버 툴팁 버그:** "상세 보기" 클릭이 빈 패널 여는 버그 → 툴팁 미리보기 전용으로 (`web/result.html`).
- **문서/구조 정리:** CLAUDE.md 경량화, spec 체계 정리(`docs/specs/_meta/`로 가이드·템플릿 이동,
  4칸 템플릿, 상태표/의식 워크플로우 제거). 미사용 폴더/파일 삭제(`deploy/`, `sandbox/`,
  `scripts/README.md`, `web/verifyhome/`, `HANDOFF_DESIGN.md`). `run.py` git 추적 추가.
- **SQLite-first / DB 모드:** `DATABASE_URL` 형식으로 PG↔SQLite 자동 분기(`config._is_pg_url`),
  `download_db.py`가 다운로드 후 `.env`를 로컬 경로로 자동 전환.
