## 2026-06-20 11:43 - 친구 채팅 응답 지연 확인 및 완화
**의도:** Sonnet 전환 후에도 친구 채팅 첫 응답이 느린 원인을 확인하고, DB에 이미 있는 단순 사실 질문의 체감 지연을 줄인다.
**최종 상태:** `app/chat.py`에서 채팅 모델은 `cfg.SONNET_MODEL`을 사용 중임을 확인. K-apt 건물 스펙 질문은 실거래/POI 쿼리와 Claude 호출 전에 즉시 SSE 응답하도록 fast-path 추가. 웹검색 tool은 호재/개발/뉴스/학군 등 외부 확인이 필요한 질문에만 붙이도록 제한. 일반 컨텍스트 답변은 max_tokens를 420으로 축소.
**건드린 파일·함수:** `app/chat.py` (`_local_fact_reply`, `_should_enable_web_search`, `apt_chat`)
**결정 / 버린 대안:** 모델만 바꾸는 방식은 큰 프롬프트·tool_use 판단·후속 질문 생성 병목을 해결하지 못해 제외. 실시간 정보가 필요한 질문의 웹검색 기능은 유지하되 기본 경로에서는 끈다.
**DB 변경:** 없음.
**함정·깨진 점:** `python.exe`가 Microsoft Store shim으로 깨져 있어 `uv run python`으로 검증. `pytest`는 uv 환경에 `pytest` 모듈이 없어 실행하지 못함. 최소 helper import도 `fastapi` 미설치로 막힘.
**미완료 TODO:** 운영 환경에서 `/api/apt/{apt_seq}/chat` 응답시간을 브라우저 Network 탭으로 확인. 필요하면 CHIPS 생성도 서버/클라이언트 정적 후보로 더 분리.
