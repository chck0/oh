## 2026-06-20 — 콜드스타트 로딩 지연 진단 + 워밍업 핑 도입

**의도:** "처음 로딩만 엄청 느리고 두 번째부터 빠름" 3대 증상(검색 prefill 칩 / 상세 패널 / 경로 폴리라인)의 원인을 코드로 규명하고, 발표(10분, 새 주소) 때 콜드스타트로 미완성처럼 보이는 문제를 완화. ODsay는 절대 호출 안 함 — 정적 분석 + SQLite 로컬 검증만.

**진단 결론:** 세 증상 모두 동일 범인 = **콜드 스타트 + Supabase pgBouncer(6543) 첫 커넥션 핸드셰이크**. 외부 API/사전로딩 무관.
- prefill: `web/search.html` `loadRecentSearches()` → `/api/workplaces/recent`가 콜드 프로세스의 첫 DB 히트. `app/db.py:48` pool 첫 getconn() = TLS+인증+유휴깨우기.
- 상세: `app/detail.py:239` `apt_detail`이 쿼리 8개 동시 병렬(`detail.py:399`), 각자 `detail.py:263` db_connect(). pool `min_size=1`(서버리스, `db.py:50`)인데 동시 8커넥션 요구 → 7개를 즉석에서 새로 염. LLM(친구 한마디)은 여기서 호출 안 함(`detail.py:299` 테이블 읽기만) — LLM 탓 아님.
- 경로: `detail.py:135` `apt_routes`는 transit_routes만 읽음(ODsay 0회). 폴리라인은 routes 응답 대기 후 렌더 → 콜드로 routes 늦으면 선도 늦음.

**최종 상태:** 워밍업 엔드포인트 + 페이지 진입 핑 도입 완료. 로컬 SQLite 검증 OK(`/api/warmup` → apts 5209·workplaces 22 예열, ok:true). `perf-warmup` 브랜치, 머지는 검토 후.

**건드린 파일·함수:**
- `app/main.py` — `GET /api/warmup` 신규. pool 첫 커넥션(SELECT 1) + `_get_cached_apts`·`_wp_mem_cache` 예열. 실패해도 페이지 무영향(try/except, 클라이언트 결과 무시).
- `web/search.html` — 로드 즉시 `fetch('/api/warmup')` 백그라운드 핑.
- `web/result.html` — 로드 즉시 `fetch('/api/warmup')` 백그라운드 핑.

**결정 / 버린 대안:**
- 채택: 워밍업 핑(추가만, 기존 동작 불변, 위험 최소).
- 보류(미구현, 위험): ① 서버리스 pool `min_size` 상향 — pgBouncer 동시성 한도 영향 → 검토 필요. ② `/api/workplaces/recent`를 `_wp_mem_cache`로 전환 — 부분 적재 시 일부만 반환되는 정합성 함정 있어 보류(워밍업으로 DB 경로도 충분히 빨라짐).
- 운영 팁: Vercel은 인보케이션마다 다른 람다라 워밍이 100% 보장은 아님 → 발표 직전 결과 페이지 한 번 열어 프로세스/pool을 따뜻하게 유지 권장. free tier면 유휴 후 첫 연결이 특히 느림.

**DB 변경:** 없음.

**함정·깨진 것:** 워밍업은 SQLite 로컬에선 핸드셰이크 비용이 없어 효과가 안 보임(11ms). 실제 효과는 Supabase 운영 환경에서 나타남.

**미완성/TODO:** 운영(Vercel+Supabase)에서 워밍업 전/후 첫 상세·경로 체감 측정. 필요 시 보류한 pool min_size 상향 검토.
