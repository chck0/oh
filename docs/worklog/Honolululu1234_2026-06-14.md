## 2026-06-14 — 정렬 칩 UI 개선 + 상세 패널 사이드 패널로 전환

**의도:** 정렬 기준 칩을 네이버맵/도보 안내판 스타일(아이콘+라벨 카드형)로 재디자인. 단지 상세 보기를 하단 모달에서 오른쪽 슬라이드 사이드 패널로 전환해 가시성 개선.
**최종 상태:**
- 정렬 칩: 3열 그리드, 아이콘 버블(#e57373 파스텔 레드) + 컬러 라벨 바, 클릭 순서 우선순위 뱃지
- 상세 패널: `detail-overlay` 제거 → `#detail-panel`이 result-layout flex 자식으로 슬라이드 (기본 420px, 드래그 리사이즈 240px~)
- 보라색 추천 핀 클릭도 동일 사이드 패널에서 렌더링 (`showDetail` → `#detail-body` 주입)
- 정렬 패널 높이 80% 축소 (패딩·칩·아이콘 모두)
**건드린 파일·함수:**
- `web/result.html`: `SORT_OPTIONS`, `initSortBar`, `.sort-panel`, `.sort-check-item`, `.sort-chip-label` CSS/JS 전면 개편. `openDetail`/`closeDetail`/`showDetail`/`renderDetailPanel`/`backToList` — detail-overlay 제거, detail-panel.open 클래스 토글로 전환. `#detail-resize-handle` HTML+JS 추가.
**결정 / 버린 대안:** 토스 스타일 개별 색상 → 사용자가 파스텔 레드 단색으로 결정. 하단 모달 → 사이드 패널로 확정.
**DB 변경:** 없음
**함정·깨진 것:** JS 인라인 style이 CSS를 override하는 문제 반복 발생 → 인라인 style 제거로 해결. `overflow:hidden`이 rank-badge 클리핑 → 제거 후 label에 `border-radius` 적용.
**미완성/TODO:** 없음

## 2026-06-14 23:40 — 왼쪽 카드 섹션 전면 간소화
**의도:** 왼쪽 패널 과부하 제거. 통계 패널 + 추천 개별 카드 + 버킷 섹션 모두 제거. 지도 핀 클릭으로 상세 확인하는 방식으로 전환.
**최종 상태:** 왼쪽 패널 = 정렬 칩 + 아파트 검색 + "추천 N곳" 헤더만.
**건드린 파일·함수:**
- `web/result.html`: `renderCards` — `renderStatsPanel` 호출 제거. 추천 개별 카드(`renderRecCard`) 렌더링 제거. 버킷 섹션 `if(false)` 처리. `_renderSortedFlat` — `renderStatsPanel` 제거.
**결정 / 버린 대안:** 통계/카드를 완전히 숨기고 지도 중심 UX로 전환.
**DB 변경:** 없음
**함정·깨진 것:** 없음
**미완성/TODO:** 없음

## 2026-06-14 23:00 — 정렬 UI 칩 방식으로 전면 개편
**의도:** 1순위/2순위/3순위 드롭다운 → pill 칩 클릭 방식으로 교체. 클릭 순서가 우선순위 자동 부여.
**최종 상태:** sort-panel이 left-panel 안에 위치, 토글 버튼으로 열기/닫기. 칩 선택 시 어두운 배경 + 보라 뱃지(①②③). 하단 힌트에 "1순위 통근 짧은순 → 2순위 가격 낮은순" 형태로 현재 정렬 표시.
**건드린 파일·함수:**
- `web/result.html`: `.sort-panel` CSS 전면 개편(칩 wrap + hint + footer). HTML — sort-group 2개 → sort-chips-wrap + sort-hint 단일 구조. `initSortBar` — 그룹별 렌더 → 전체 칩 flat 렌더. `_refreshSortUI` — 힌트 텍스트 "N순위 라벨 → ..." 업데이트 추가.
**결정 / 버린 대안:** 그룹 타이틀(기본/도보) 제거 — 칩이 많지 않아 구분 불필요.
**DB 변경:** 없음
**함정·깨진 것:** 없음
**미완성/TODO:** 없음

## 2026-06-14 21:10 — 정렬바 조건요약 줄로 이동 + 통근시간 색상 범례 통일
**의도:** 정렬바(정렬순서)를 별도 줄에서 조건 요약 바(summary-bar) 같은 줄 우측으로 이동. 카드섹션 통근시간 버킷 레이블·색상을 지도 범례와 완전 통일.
**최종 상태:** summary-bar + sort-bar를 combined-bar wrapper로 묶어 한 줄 배치. 백엔드 버킷 3구간(≤30/31~45/46~60)으로 변경, 프론트 minibar에 범례 색상(초록/파랑/주황) 및 dot 표시 추가.
**건드린 파일·함수:**
- `web/result.html`: `.combined-bar` CSS 추가, `.summary-bar` flex:1로, `.sort-bar` border-left 구분선. minibar CSS grid 4열(dot 추가), `.minibar-row__dot` 신규. `renderStatsPanel` — `bucketColor`/`bucketLabel` 함수 신규(숫자 파싱 기반으로 라벨 포맷 무관하게 색상 결정).
- `app/ai.py`: `make_buckets` — 10분 단위 루프 → 3구간 고정(0~30, 30~45, 45~max). `bucket_label` — "30분 이하"/"31~45분"/"46~{e}분" 반환.
**결정 / 버린 대안:** 라벨 문자열 직접 비교 대신 숫자 파싱 방식 채택 — 구버전 캐시 데이터에도 올바른 색상 적용.
**DB 변경:** 없음
**함정·깨진 것:** 브라우저/서버 캐시로 변경 미반영 현상 → 서버 재시작 + Ctrl+Shift+R로 해결.
**미완성/TODO:** 없음

## 2026-06-14 20:00 — 정렬 드롭다운 바 복원 (1순위/2순위/3순위)
**의도:** 이전에 제거했던 정렬 바를 main 기준으로 복원.
**최종 상태:** sort-bar HTML 재추가, initSortBar null 가드 제거.
**건드린 파일·함수:** `web/result.html` — sort-bar HTML 복원, `initSortBar` `?.` 제거.
**결정 / 버린 대안:** 없음.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 19:30 — 범례 배경 흰색으로 변경 (지도 조화)
**의도:** 검정 반투명 범례가 카카오맵 주변 UI와 이질감 → 흰색 반투명으로 교체해 조화 개선.
**최종 상태:** 흰 배경(94% 불투명) + 연한 테두리 + 부드러운 그림자. 텍스트 색도 흰→어두운 slate 계열로 전환.
**건드린 파일·함수:** `web/result.html` — `#map-legend` CSS 배경·텍스트·구분선·그림자 전면 교체.
**결정 / 버린 대안:** 없음.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 19:00 — 오피스 마커 SVG 픽토그램 원형 핀으로 교체
**의도:** 집 핀과 모양이 겹쳐 구분이 안 돼, 마커 전체를 건물 실루엣 SVG + 검정 원형 배경으로 완전 교체.
**최종 상태:** 오피스1 = 고층빌딩형(A) SVG, 오피스2 = 복합빌딩형(C) SVG. 검정 원 + 흰 테두리 + 빨간 숫자 배지.
**건드린 파일·함수:**
- `web/result.html`: `.pin-workplace-*` CSS 전면 제거 → `.pin-workplace-wrap/2-wrap` 최소 스타일만 유지. JS CustomOverlay content — 건물 모양 직사각형 구조 → 원형 배경 SVG 픽토그램으로 교체.
**결정 / 버린 대안:** 집 핀과 동일한 지붕+몸체 구조, 원형+건물아이콘, 다양한 배색 등 시도 → 검정 원형 배경이 가장 대비 명확.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 17:30 — 지도 UI 개선 (범례·오피스 마커)
**의도:** 지도 가시성 향상. 범례 2배 확대 → 다크 배경으로 재배색. 오피스 마커 텍스트 변경 + 크기·배색 조정.
**최종 상태:** 범례 다크 반투명(blur) + 구분선. 오피스 마커 흰 배경+차콜 테두리로 주변 집 모양 핀과 조화.
**건드린 파일·함수:**
- `web/result.html`: `#map-legend` CSS 다크 배경화. 오피스 마커 CSS(`.pin-workplace-*`) 전면 개편. JS `content` 문자열 W1/W2 직장 → 🏢 오피스 1/2 변경.
**결정 / 버린 대안:** 앰버+에메랄드 배색 시도했으나 주변 핀과 충돌 → 흰 배경+무채색 테두리로 변경.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 16:30 — 검색 조건 3개로 간소화 (평형·세대수·준공연도 제거)
**의도:** 검색 진입 장벽 낮추기. 직장주소·가격범위·통근시간 3개만 남기고 나머지 제거.
**최종 상태:** ④관심평형 ⑤단지세대수 ⑥준공연도 섹션 HTML 삭제. 번호 1·2·3 그대로 유지.
**건드린 파일·함수:** `web/search.html` — 섹션 ④⑤⑥ HTML 제거.
**결정 / 버린 대안:** 평형·세대수·준공연도는 결과 페이지 필터로 이동 가능 (미구현).
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 16:00 — 검색 페이지 가격 입력 → 듀얼 슬라이더로 교체
**의도:** 숫자 직접 입력(억/만원 4칸) → 마우스로 드래그하는 듀얼 핸들 범위 슬라이더로 교체. UX 개선.
**최종 상태:** 0~20억 범위, 5000만원 단위 step. 최소/최대 레이블 실시간 업데이트. 기존 hidden input + URL 파라미터 호환 유지.
**건드린 파일·함수:**
- `web/search.html`: `.price-range-row` CSS → `.price-slider-wrap`/`.price-dual-range` CSS 교체. HTML 슬라이더 + hidden inputs 구조로 변경. `validatePrice` 단순화, `autofillMinFromMax` 제거, `onPriceSlider()` 신규 추가. `prefillFromParams`에서 슬라이더 값 복원.
**결정 / 버린 대안:** hidden input 유지로 submit/URL 파라미터 기존 로직 무수정 재사용.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 15:30 — 카드 UI 가시성 개선 (compact 추천카드 + 한 줄 일반카드)
**의도:** 왼쪽 카드 섹션이 정보 과밀 → 추천카드 3줄 압축 + AI 한마디 접기/펼치기, 일반카드 한 줄 row 형태로 재설계.
**최종 상태:** CSS/JS 변경 완료.
**건드린 파일·함수:**
- `web/result.html`: `.rec-card` CSS 전면 개편(compact 3-row), `.list-card` CSS → flex 한 줄 row. `renderRecCard` 친구 코멘트 토글 구조 추가. `renderListCard` 단지명+메타(좌) / 가격+통근(우) 2열 구조. `_toggleFriend()` 신규 추가.
**결정 / 버린 대안:** AI 코멘트 완전 제거 대신 접기/펼치기로 유지 — 서비스 차별점 보존.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 14:45 — 결과 페이지 정렬순서 바 제거
**의도:** 결과 페이지 상단 1순위/2순위/3순위 정렬 드롭다운 바 제거.
**최종 상태:** `sort-bar` div 삭제 완료.
**건드린 파일·함수:** `web/result.html` — `#sort-bar` HTML 제거.
**결정 / 버린 대안:** 없음.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 14:30 — 바텀시트 레이아웃 롤백 (7bad999으로 복원)
**의도:** 풀스크린+바텀시트 레이아웃이 마음에 안 들어 이전 버전(호갱노노 핀+범례, 왼쪽 카드 패널 유지)으로 롤백.
**최종 상태:** `web/result.html`, `web/search.html` → commit 7bad999 기준으로 복원.
**건드린 파일·함수:** `web/result.html`, `web/search.html` (git checkout 7bad999)
**결정 / 버린 대안:** 바텀시트 UX 폐기.
**DB 변경:** 없음.
**함정·깨진 것:** 없음.
**미완성/TODO:** 없음.

## 2026-06-14 14:00 — 결과 페이지 풀스크린 지도 + 바텀시트 레이아웃 리디자인
**의도:** 왼쪽 카드 패널을 완전 제거하고, 지도 풀스크린 위에 슬라이드업 바텀시트로 카드 목록을 표시. 호갱노노 스타일 집 모양 핀(통근시간 표시, 컬러 코딩).
**최종 상태:** `web/result.html` HTML/CSS/JS 교체 완료. 바텀시트 드래그 핸들, 4개 정렬 칩, `#bs-count` 카운트 표시. 핀 컬러: 초록(≤30분)/파랑(≤45분)/주황(≤60분)/빨강(>60분)/보라(추천).
**건드린 파일·함수:**
- `web/result.html`: `.left-panel`/`.sort-bar`/`.resize-handle` CSS 제거 → `.bottom-sheet`/`.bs-*` CSS 추가. HTML에서 sort-bar + left-panel + resize-handle 제거 → `.map-panel` 안에 `.bottom-sheet` 추가. `initSortBar()` → `#bs-count` 업데이트. `bsSort()` 신규 추가. `initResizeHandle()` no-op화. `updateSortDropdowns`/`onSortChange`/`applySortOrder`/`resetSort` no-op화 (sort-1/2/3 요소 삭제로 인한 에러 방지). `_renderSortedFlat` → `#bs-count` 동기화 추가.
- `web/search.html`: main 브랜치와 동일하게 리버트.
- `.env.example`: .gitignore에서 제외.
**결정 / 버린 대안:** 기존 sort-1/2/3 드롭다운 함수들은 삭제 대신 no-op 처리 — `resetSort` 등 외부에서 호출되는 케이스가 있어 런타임 에러 방지 우선.
**DB 변경:** 없음.
**함정·깨진 것:** sort-1/2/3 요소 제거 후 `updateSortDropdowns` 등에서 `.value` 호출 시 null 에러 → no-op 처리로 해결.
**미완성/TODO:** 바텀시트 드래그가 마우스 이벤트 기반 — 모바일 터치 지원 미적용.

## 2026-06-14 01:10 — 결과 페이지 필터 패널 + 검색 페이지 간소화
**의도:** 검색 페이지에서 평형·세대수·건축연도를 제거하고 결과 페이지 왼쪽 패널에 칩 기반 필터 패널로 이동. 정렬 드롭다운(1순위·2순위·3순위) 제거 → 4개 칩(추천순/통근시간/가격낮은순/신축순)으로 교체. 버킷 섹션(통근시간대별 그룹) 제거 → 플랫 카드 리스트.
**최종 상태:** 로컬 서버에서 정상 동작 확인. 138곳 결과, 필터 패널 칩 클릭 시 클라이언트 사이드 즉시 필터링 동작.
**건드린 파일·함수:**
- `web/search.html`: ④ 관심 평형·⑤ 세대수·⑥ 건축연도 섹션 제거. pyeong 검증 제거. `prefillFromParams`에서 pyeong/mincnt/build_year 복원 코드 제거. URL params에서 `pyeong_types`, `min_kaptdaCnt`, `build_year_min` 제거.
- `web/result.html`: `initFilterPanel`, `fpFilter`, `fpSort`, `_sortCards`, `applyClientFilters`, `_renderSortedFlat` 신규 추가. `initSortBar`, `updateSortDropdowns`, `onSortChange`, `applySortOrder`, `resetSort` 제거. `renderCards` → 플랫 리스트 + `applyClientFilters()` 호출로 교체. 상단 sort-bar HTML 제거. 필터 패널 HTML/CSS 추가. Tabler Icons CSS 추가.
**결정 / 버린 대안:**
- 통근시간·가격은 백엔드 재계산이 필요해 클라이언트 필터 불가 → 처음엔 자물쇠 UI로 표시했다가 사용자 혼란 우려로 완전 제거.
- 정렬 3순위 드롭다운 시스템 → 사용자가 "너무 복잡하다"고 피드백 → 4개 단일 칩으로 교체.
- 버킷 섹션(아코디언) → "없애달라"는 명시적 요청 → 플랫 리스트로.
- ALL_PYEONG를 백엔드에 항상 전송 → 클라이언트에서 평형 필터.
**DB 변경:** 없음
**함정·깨진 것:** `commute_min` → 실제 필드명은 `total_time_min`. `_sortCards`에서 replace_all로 수정.
**미완성/TODO:** 없음

## 2026-06-14 00:23 — 로컬 환경 셋업 + worklog 온보딩
**의도:** 로컬 Python 환경 구성, Supabase에서 SQLite DB 다운로드, 서버 기동 확인.
**최종 상태:** `data/apartment.db` (146.3MB, 675,446행) 다운로드 완료. 서버 정상 기동.
**건드린 파일·함수:** `.env` (DATABASE_URL 로컬 경로로 자동 업데이트), `docs/worklog/.author` 생성.
**결정 / 버린 대안:** 데모 DB 시드 → 실제 Supabase URL 입력 후 실제 DB로 교체.
**DB 변경:** 없음
**함정·깨진 것:** `requirements.txt` 한글 주석 인코딩 오류 → `pip install -r requirements.txt --encoding utf-8` 로 해결.
**미완성/TODO:** 없음
