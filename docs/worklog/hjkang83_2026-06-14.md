# 작업 일지 — hjkang83 (2026-06-14)

최신 작업이 맨 위.

---

## 2026-06-15 02:55 — 단지 비교표 항목 교체 (저가근거·6개월변동 → 평당가·거래건수)

**의도:** 단지 비교 모달에서 '저가 근거'(why_tags)·'6개월 변동'(price_chg_6m_pct)이 데이터 없어 모든 단지 빈칸으로 표시됨(IMG_8095) → 데이터가 항상 있는 비교 가능 항목으로 대체.

**최종 상태:** 비교표에서 두 행 제거하고 **평당가(최저가÷평수, 만/평)**·**거래 건수(최근 3개월)**로 교체. 평당가는 최저 실거래가 옆에 배치, 최솟값 강조(compare-best). API 확인: 평당가 3110/3078/2751만, 거래 3건 정상 계산.

**건드린 파일:** `web/result.html` — `_renderCompareTable` rows 배열 (평당가 pppOf 추가, 저가근거/6개월변동 → 평당가/거래건수)

**결정 / 버린 대안:**
- 평당가: 평형이 달라도 정규화 비교 가능 → 가장 유용. 거래건수: 거래 활발도(환금성) 비교
- ❌ 6개월 변동 유지: trade_history 데이터 부족으로 대부분 빈칸 → 신뢰 저하

**DB/API 변경:** 없음 (result.html 단일, 기존 카드 필드 활용)

**함정·깨진 것:** 샌드박스는 Kakao SDK 차단으로 result 카드 렌더 중단 → 비교 모달 UI 시각검증 불가(코드+API 데이터로 검증).

**미완성/TODO:** 없음

## 2026-06-14 22:45 — 결과 정렬 패널 접기/펼치기 토글

**의도:** 결과 화면 좌측 정렬 기준 패널(통근시간 짧은순·가격낮은순 등)이 카드 위에 항상 펼쳐져 매물 공간을 잡아먹음 → 접기 버튼으로 필요시 접어 매물을 더 보게.

**최종 상태:** sort-panel 헤더 "정렬 기준 ▾" 클릭으로 접기/펼치기. 접으면 칩·힌트 숨김(헤더만 남음)→카드 공간 확보. 상태 localStorage 기억(기본 펼침). 라이브 검증: 펼침→접음(칩 숨김, ls=0)→펼침(ls=1).

**건드린 파일:**
- `web/result.html` — 기존 미완성 토글 인프라(`.sort-toggle-btn` CSS, `toggleSortPanel()`) 완성: 헤더에 토글 버튼 추가, `.sort-panel:not(.open)` 칩/힌트 숨김 CSS, 렌더 시 localStorage로 초기 상태 복원

**결정 / 버린 대안:**
- 기본=펼침(현재 동작 유지) + 상태 기억 → 한 번 접으면 다음에도 접힌 채 매물 우선
- 기존에 남아있던 sort-toggle-btn CSS·toggleSortPanel 함수 재활용 (신규 최소화)

**DB/API 변경:** 없음 (result.html 단일 파일)

**미완성/TODO:** 없음

## 2026-06-14 22:35 — 상세조건 칩 한 줄 배치 (컴팩트)

**의도:** 상세조건 칩이 6/5/5개라 줄바꿈됨 → 항목별 한 줄에 들어가도록 칩 크기 축소.

**최종 상태:** `.adv-section` 스코프로 칩 컴팩트화(패딩 7px 12px, font 13px, gap 6px) + 체크박스 네모 숨김(선택은 빨강 테두리). `flex-wrap:nowrap + overflow-x:auto`로 좁은 화면은 가로 스크롤. 검증: 평형6·준공5·세대수5 모두 각 1줄.

**건드린 파일:** `web/search.html` (.adv-section .check-chip CSS만)

**DB/API 변경:** 없음

**미완성/TODO:** 없음

## 2026-06-14 22:25 — spec-37 검색 상세조건 펼침 (평형·준공연도·세대수)

**의도:** 검색 화면은 그대로 두고, 통근시간 아래 접이식 "상세조건"으로 평형·준공연도·세대수를 골라 그 조건으로 분석. 기존엔 평형 6종 하드코딩 + 준공/세대수 미전송이라 좁혀서 검색 불가.

**최종 상태:** search.html에 접이식 섹션 추가(기본 접힘). 평형 체크박스(기본 전체)·준공연도·세대수 라디오. 제출 시 선택값을 result URL에 직렬화 → result.html이 이미 /api/search로 전달(백엔드 기지원). 라이브 검증: 제출 URL pyeong_types=20평대,30평대/build_year_min=2015/min_kaptdaCnt=500, 조건 다시 입력 시 자동 펼침+복원. 전체 410 passed.

**건드린 파일:**
- `web/search.html` — 상세조건 HTML(기존 .check-chip/.radio 스타일 재사용) + .adv-toggle CSS + 토글 JS + 제출 직렬화(평형 하드코딩 제거, build_year_min/min_kaptdaCnt 추가) + prefill 자동 펼침
- `docs/specs/37-search-advanced-conditions.md` — Approved→Implemented

**결정 / 버린 대안:**
- 평형 기본=전체 선택(현재 동작 유지). 0개 선택 시 전체 폴백(빈 결과 방지)
- 백엔드/result.html 무변경 — 이미 세 조건 모두 URL→API 지원. search.html 수집만 추가
- 기존에 남아있던 prefill 복원 코드(name=pyeong/mincnt/build_year_min)와 .check-chip CSS 재활용 → 신규 코드 최소화

**DB 변경:** 없음 · **API 변경:** 없음

**함정·깨진 것:** address 입력은 readonly(주소찾기 팝업)라 테스트 시 fill 대신 value 직접 세팅 필요.

**미완성/TODO:** 없음


## 2026-06-14 22:10 — test_ai 버킷 테스트 8건 갱신 (3구간 로직 반영)

**의도:** 최근 Honolululu1234 머지가 `make_buckets`를 10분 단위(4구간)→3구간(≤30/31~45/46~max)으로 의도적으로 바꿨는데(작성자 worklog L58·61에 명시) test_ai.py를 안 고쳐 8건 실패. 의도된 변경이므로 테스트를 새 로직에 맞춤.

**최종 상태:** 전체 410 passed (이전 8 failed → 0). 로직은 그대로 두고 테스트만 갱신.

**건드린 파일:**
- `tests/test_ai.py` — TestMakeBuckets(60→3구간), TestBucketLabel("30분 이하"/"31~45분"/"46~60분"), TestAssignBucket(3구간 경계), TestBuildRecommendations(buckets==3, label "30분 이하")

**결정 / 버린 대안:**
- ❌ make_buckets 로직 되돌리기: 작성자가 의도적으로 바꾼 것(worklog 명시) → 테스트를 맞추는 게 맞음
- ✅ 새 3구간 경계에 맞춰 assign_bucket 기대값 재계산 (45→bucket1, 50→bucket2, 60→bucket2)

**DB 변경:** 없음

**함정·깨진 것:** assign_bucket(45)는 (30,45] 경계라 bucket 1 (이전 4구간에선 bucket 2였음) — 경계 동작 바뀐 점 테스트에 반영.

**미완성/TODO:** 없음

---

## 2026-06-14 22:00 — "조건 다시 입력" 시 기존 조건 사라지는 버그 수정

**의도:** 결과 화면에서 "조건 다시 입력"을 누르면 직전에 넣은 가격·통근시간이 기본값으로 초기화되는 문제. search.html이 result.html의 URL 파라미터로 복원하게 돼 있는데 복원이 중간에 끊김.

**최종 상태:** 근본 원인은 **변수 초기화 순서(TDZ)**. `const PRICE_MAX_MAN`이 prefill 코드보다 뒤(803줄)에 정의돼, prefill이 `onPriceSlider()`를 호출하는 순간(749줄) 아직 초기화 안 된 상수를 참조 → `ReferenceError`로 prefill IIFE가 중단됨. 그래서 슬라이더 값만 세팅되고(중단 전) 가격 라벨·숫자칸·통근시간 칩 복원은 실행 못 함. → `PRICE_MAX_MAN` 정의를 prefill 앞으로 이동해 해결. 라이브 검증: 주소·슬라이더·가격라벨(7억/3억)·통근칩(40) 전부 복원, JS 에러 0.

**건드린 파일:**
- `web/search.html` — `const PRICE_MAX_MAN = 200000;`을 가격 슬라이더 정의부(803)에서 prefill IIFE(730) 앞으로 이동

**결정 / 버린 대안:**
- ❌ prefill 블록 전체를 스크립트 끝으로 이동: 더 크고 위험. 상수 1줄 이동이 최소·확실.
- ✅ 상수만 앞으로 이동 (TDZ 해소). 함수(onPriceSlider/toggleWp2)는 선언이라 호이스팅돼 문제 없음.

**DB 변경:** 없음 (순수 프론트)

**함정·깨진 것:**
- prefill의 pyeong/세대수/준공연도 복원 코드는 현재 폼에 해당 입력이 없어 동작 안 함(stale no-op). 이번 버그와 무관해 그대로 둠.
- ⚠️ **별도 발견(이번 수정과 무관)**: `test_ai.py` 버킷 테스트 8건이 main에서 실패 중. `make_buckets(60)`이 `[(0,30),(30,45),(45,60)]`(3버킷) 반환하는데 테스트는 4버킷(10분 단위) 기대 → 최근 Honolululu1234 머지가 버킷 로직 변경 후 테스트 미갱신. 의도된 변경인지 확인 필요(별도 처리).

**미완성/TODO:**
- 위 test_ai 버킷 회귀: 새 로직(15분 상위 버킷)이 의도된 것이면 테스트 갱신, 아니면 로직 복구 — 작성자/사용자 확인 후 처리.
