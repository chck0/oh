## 2026-06-20 — fix(map): 지도 오른쪽 흰 공백 버그 수정

**의도:** 검색 결과 화면에서 Kakao 지도 오른쪽에 흰 공백이 생기는 레이아웃 버그 수정.
**최종 상태:** 지도가 map-panel(flex:1)을 100% 채워서 렌더링됨. 로컬호스트에서 정상 확인.
**건드린 파일·함수:**
- `web/result.html` — `initMap()`: `await requestAnimationFrame` 추가 + `kakaoMap.relayout()` 호출
**결정 / 버린 대안:** `setTimeout(0)` 대신 `requestAnimationFrame` 사용 — 브라우저가 실제로 렌더 트리를 계산한 직후 시점을 정확히 잡기 위함. `relayout()` 단독 호출 없이 rAF만 써도 가능하지만 양쪽 모두 추가해 방어적으로 처리.
**DB 변경:** 없음
**함정·깨진 것:** `#main`을 `display:none`→`display:flex`로 전환 직후 `await kakaoReady`(이미 resolved)가 거의 동기적으로 통과해 flex 레이아웃 미계산 상태에서 Kakao Map이 초기화됨 → 지도가 min-width(320px) 기준으로만 렌더 → 오른쪽 흰 공백.
**미완성/TODO:** 없음

## 2026-06-20 00:34 — merge/ui-honolululu: 좌측 패널 + 카드 컬럼 UI 병합

**의도:** Honolululu1234 브랜치의 좌측 패널 UI(상세조건 슬라이더 필터 + 새 정렬 드롭다운 + 별도 카드 컬럼)를 main 기반 merge/ui-honolululu 브랜치에 병합. main의 spec-38, spec-12, spec-42, spec-22 등 기존 기능은 손대지 않음.
**최종 상태:** left-panel이 상세조건 필터(평형/준공연도/세대수 슬라이더) + 정렬 드롭다운으로 재구성됨. 카드 목록은 별도 card-column(접힘/펼침 토글)으로 분리. 필터 조작 시 지도 핀 실시간 표시/숨김 연동. search.html의 상세조건 섹션(평형 체크박스 등) 제거.
**건드린 파일·함수:**
- `web/result.html` — CSS: `.left-panel`, `.card-column`, `.cards-show-btn`, `.adv-filter-panel`, `.adv-year-*`, `.sort-panel`, `.sort-panel-body`, `.sort-chips-wrap`, `.sort-check-item` 전체 교체 및 추가
  - HTML: `left-panel` 내부를 adv-filter-panel + sort-panel로 교체; `card-column` 신규 추가 (cards-panel + compare-bar 이동)
  - JS: `toggleCardColumn()`, `toggleSortPanel()` 교체, `_advFilter` 상태 + `_initPyeongSlider/YearSlider/UnitsSlider` + `resetAdvFilter()` + `_applyAdvFilter()` + `_filterCardsByAdv()` 추가; `renderCards`에 allCards 분기 + 슬라이더 초기화 호출 추가; `_refreshSortUI`에 sort-panel-reset-btn 표시 추가
- `web/search.html` — 상세조건 섹션(평형·준공연도·세대수 체크박스/라디오) HTML/CSS/JS 전체 제거; submit 시 pyeong = ALL_PYEONG 고정
**결정 / 버린 대안:** Step 3(left-panel) + Step 4(card-column)를 HTML 구조가 묶여있어 한 번에 처리. git merge/rebase 대신 surgical patch 방식 유지 (두 브랜치 공통 조상 없음).
**DB 변경:** 없음
**함정·깨진 것:** `sort-toggle-btn` 요소가 HTML에서 제거됐으나 CSS에 잔존 — 무해함. main의 `initSortBar`에서 sort-panel `.open` 클래스를 자동 설정하던 로직 제거 (새 dropdown 방식과 충돌).
**미완성/TODO:** 브라우저에서 실제 검색 결과 기준 슬라이더 동작 육안 확인 필요. 핀 색상(Step 2, 이미 적용됨) + 핀 hover 애니메이션(spec-38, 보존됨) 교차 테스트 권장.
