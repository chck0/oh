# Spec NN: [기능 이름]

> 상태: Draft → Implemented | 작성일: YYYY-MM-DD | 브랜치: name/feature

## 1. Why (왜)

- 무슨 문제를 해결하나 / 사용자가 얻는 것:

## 2. Scope (범위)

- **포함:**
- **제외(안 함):**

## 3. 설계 (어떻게)

- 건드리는 파일:
- DB 변경: (없으면 "없음". 있으면 `scripts/*.sql`)
- API 변경: (없으면 "없음")
- 핵심 로직 / 주의할 엣지케이스:

## 4. 완료 조건 (Acceptance Criteria)

- [ ] AC1:
- [ ] AC2:
- [ ] 로컬(SQLite) + Vercel(Supabase) 양쪽 동작
